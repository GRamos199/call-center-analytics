# =============================================================================
# Deploy to AWS Workflow
# Builds Docker image and deploys to AWS ECS
# =============================================================================

name: 🚀 Deploy to AWS

on:
  push:
    branches: [main]
    paths-ignore:
      - 'terraform/**'
      - '*.md'
      - '.github/workflows/terraform.yml'
      - '.github/workflows/test.yml'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: call-center-analytics
  ECS_CLUSTER: call-center-analytics-dev-cluster
  ECS_SERVICE: call-center-analytics-dev-service
  CONTAINER_NAME: call-center-analytics

jobs:
  # ===========================================================================
  # Check AWS Credentials
  # ===========================================================================
  check-secrets:
    name: 🔐 Check AWS Credentials
    runs-on: ubuntu-latest
    outputs:
      has-aws-credentials: ${{ steps.check.outputs.has-credentials }}

    steps:
      - name: 🔍 Check if AWS credentials are configured
        id: check
        env:
          AWS_KEY: ${{ secrets.AWS_ACCESS_KEY_ID }}
        run: |
          if [ -n "$AWS_KEY" ]; then
            echo "has-credentials=true" >> $GITHUB_OUTPUT
            echo "✅ AWS credentials are configured"
          else
            echo "has-credentials=false" >> $GITHUB_OUTPUT
            echo "⚠️ AWS credentials not configured - skipping deployment"
          fi

  # ===========================================================================
  # Build and Push Docker Image
  # ===========================================================================
  build:
    name: 🐳 Build & Push
    runs-on: ubuntu-latest
    needs: check-secrets
    if: needs.check-secrets.outputs.has-aws-credentials == 'true'
    outputs:
      image: ${{ steps.build-image.outputs.image }}

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: 🔑 Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: 🏷️ Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}
          tags: |
            type=sha,prefix=
            type=ref,event=branch
            type=raw,value=latest,enable={{is_default_branch}}

      - name: 🔧 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🐳 Build and push Docker image
        id: build-image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 📝 Output image URI
        run: |
          echo "Image: ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}"

  # ===========================================================================
  # Build Skipped Notice
  # ===========================================================================
  build-skipped:
    name: 🐳 Build (Skipped - No AWS Credentials)
    runs-on: ubuntu-latest
    needs: check-secrets
    if: needs.check-secrets.outputs.has-aws-credentials != 'true'

    steps:
      - name: ⚠️ AWS Credentials Required
        run: |
          echo "============================================="
          echo "⚠️  DEPLOYMENT SKIPPED"
          echo "============================================="
          echo ""
          echo "AWS credentials are not configured."
          echo ""
          echo "To enable deployment, add these secrets:"
          echo "  Repository → Settings → Secrets → Actions"
          echo ""
          echo "  • AWS_ACCESS_KEY_ID"
          echo "  • AWS_SECRET_ACCESS_KEY"
          echo ""
          echo "============================================="

  # ===========================================================================
  # Deploy to ECS
  # ===========================================================================
  deploy:
    name: 🚀 Deploy to ECS
    runs-on: ubuntu-latest
    needs: [check-secrets, build]
    if: needs.check-secrets.outputs.has-aws-credentials == 'true'
    environment: production

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: 🔑 Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: 📋 Download task definition
        run: |
          aws ecs describe-task-definition \
            --task-definition ${{ env.ECS_SERVICE }}-task \
            --query taskDefinition > task-definition.json

      - name: 🔄 Update task definition with new image
        id: task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: task-definition.json
          container-name: ${{ env.CONTAINER_NAME }}
          image: ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}

      - name: 🚀 Deploy to Amazon ECS
        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
        with:
          task-definition: ${{ steps.task-def.outputs.task-definition }}
          service: ${{ env.ECS_SERVICE }}
          cluster: ${{ env.ECS_CLUSTER }}
          wait-for-service-stability: true

      - name: ✅ Deployment successful
        run: |
          echo "🎉 Deployment completed successfully!"
          echo "The application should be available at your ALB DNS name."
